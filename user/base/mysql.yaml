apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-mysql
  labels:
    app: user-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-mysql
  template:
    metadata:
      labels:
        app: user-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root1234
            - name: MYSQL_DATABASE
              value: hospital_user
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: user-mysql
  labels:
    app: user-mysql
spec:
  type: NodePort
  selector:
    app: user-mysql
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30091
