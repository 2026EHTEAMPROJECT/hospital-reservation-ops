apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-mysql
  labels:
    app: booking-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: booking-mysql
  template:
    metadata:
      labels:
        app: booking-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root1234
            - name: MYSQL_DATABASE
              value: hospital_booking
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: booking-mysql
  labels:
    app: booking-mysql
spec:
  type: NodePort
  selector:
    app: booking-mysql
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30092
