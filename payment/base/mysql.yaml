apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-mysql
  labels:
    app: payment-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-mysql
  template:
    metadata:
      labels:
        app: payment-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root1234
            - name: MYSQL_DATABASE
              value: hospital_payment
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: payment-mysql
  labels:
    app: payment-mysql
spec:
  type: NodePort
  selector:
    app: payment-mysql
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30093
